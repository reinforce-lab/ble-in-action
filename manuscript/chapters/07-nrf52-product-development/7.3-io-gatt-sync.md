## 7.3 IO、製品状態、GATTを同期する {#sec:ch7-io-gatt-sync}

割り込みやタイマーから処理を実行できても、GPIO、センサー値、GATT Valueを別々に更新すると、外部から見える状態と実際の製品状態がずれます。製品内部に状態の正本を置き、物理的な変化とBLEからの要求を同じ更新経路へ集めます。

状態や設定の同期に加えて、完了まで時間のかかるコマンドを非同期に扱う方法も整理します。
