## 10.1 BLEビーコンの仕組み {#sec:beacon-overview}

**BLEビーコン**はペアリング・接続を行わず、アドバタイジングパケットのみを繰り返し送信するデバイスです。スキャン側はパケットを受け取るだけで情報を得られるため、接続オーバーヘッドがなく電池が長持ちします。

```
ビーコン (ペリフェラル)                スキャナ (セントラル)
┌───────────────────────┐             ┌─────────────────────┐
│  ADV_NONCONN_IND 送信   │ ─────────▶ │  受信・解析           │
│  100ms ごとに繰り返し    │             │  接続しない           │
└───────────────────────┘             └─────────────────────┘
```

接続型ビーコン（スキャンレスポンス）も存在しますが、一般的なビーコンは **ADV_NONCONN_IND**（コネクタブルでない広告）を使います。


## iBeacon

**iBeacon** はAppleが2013年に発表したビーコンフォーマットです。アドバタイジングデータのManufacturer Specific Dataフィールド（AD Type: 0xFF）を使います。

```
iBeacon アドバタイズペイロード（合計26バイト）
┌────┬────┬────────────────────────────────────────────┐
│ 02 │ 15 │ UUID (16バイト) │ Major (2B) │ Minor (2B) │ TxPower (1B) │
└────┴────┴────────────────────────────────────────────┘
  ↑    ↑
Apple CompanyID (0x004C) + iBeaconサブタイプ + 長さ
```

| フィールド | サイズ | 用途 |
|---|---|---|
| UUID | 16バイト | アプリ / 組織の識別子 |
| Major | 2バイト | エリア区分（例：フロア番号） |
| Minor | 2バイト | 個体識別（例：棚番号） |
| TxPower | 1バイト | 1m距離での受信電力（dBm）。距離推定に使用 |

### 距離推定

iOSは TxPower と実測RSSI から `CLProximity` を計算します。

$$
d = A \times \left(\frac{\text{RSSI}}{\text{TxPower}}\right)^B + C
$$

ただし、RSSIは電波の反射、障害物、人体、機器の向きによって大きく変動するため、距離推定の誤差を一律に±何 mと示すことはできません。`CLProximity`も正確な距離ではなく、近接状態を大まかに分類するために使います。

## AndroidとFind Hub Network

Android側で現在使われているビーコンの製品基盤には、GoogleのFind Hub Networkがあります。単体の紛失防止タグやイヤホンなどが、暗号化された識別情報をBLEアドバタイジングとして送ります。周囲のAndroidデバイスがそれを検出し、所有者がFind Hubアプリから位置を確認できる探索網を作ります。

Find Hub Networkは、任意の自作ビーコンから利用できる汎用サービスではありません。対応製品にはGoogleへの提案、Fast PairとFind Hub Networkへの適合、試験、認証、出荷承認が必要です。そのため、本章ではFind Hub Network対応タグそのものは実装せず、Androidで一般のBLEアドバタイジングを受信する方法を扱います。


**まとめ**

- BLEビーコンはADV_NONCONN_INDで接続なしに情報を送信する
- iBeaconはApple規格。UUID/Major/MinorとTxPowerでエリア・近接判定
- Android側には、認証された製品が参加するFind Hub Networkがある
- 距離推定はあくまで目安。環境依存が大きい

次節ではnRF52840をiBeaconとして動作させるファームウェアを実装します。

<!-- 出典候補（清書時に脚注・参考文献の形式を整理）:
Google for Developers, “Find Hub Network Accessory Specification,” Version 1.3,
https://developers.google.com/nearby/fast-pair/specifications/extensions/fmdn
Google for Developers, “Partner Integration Guide for Google's Find Hub Network,”
https://developers.google.com/nearby/fast-pair/landing-page-find-hub
2026年7月29日参照。
-->
