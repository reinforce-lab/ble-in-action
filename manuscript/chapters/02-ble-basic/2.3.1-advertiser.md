### アドバタイザーは短いパケットを繰り返し送る {#sec:advertiser}

最初に、存在を知らせる側の動きを見ます。接続前の周辺機器は、アドバタイザーとして短いパケットを繰り返し送信します。受信相手から常に問い合わせを受け付けるのではなく、自分で決めた時刻に送信し、それ以外の時間は無線回路を休ませます。

#### 三つのチャンネルへ順に送る

本章では、基本となるLegacy Advertisingだけを扱います。接続前の周辺機器はアドバタイザーとして、アドバタイジングイベントごとに短いパケットを送信します。基本例では、37、38、39の三つのプライマリアドバタイジングチャンネルへ順に送ります。

```text
チャンネル37で送信
    ↓ 同じチャンネルで短時間受信
    Scan Requestまたは接続要求を待つ
    ↓
チャンネル38で送信
    ↓ 同じチャンネルで短時間受信
    Scan Requestまたは接続要求を待つ
    ↓
チャンネル39で送信
    ↓ 同じチャンネルで短時間受信
    Scan Requestまたは接続要求を待つ
    ↓
次のアドバタイジングイベントまで休む
```

三つのチャンネルでの送受信をまとめたものが、一回のアドバタイジングイベントです。使用するチャンネルは設定で選べますが、本章では三つすべてを使う構成を基本とします。

接続可能またはスキャン可能なLegacy Advertisingでは、アドバタイザーは各パケットを送った直後に同じチャンネルで短時間受信します。Scan Requestを受信した場合はScan Responseを返し、接続要求を受信した場合はAdvertisingを終えて接続へ移ります。何も受信しなければ次のチャンネルへ進み、イベントが終わると次の送信時刻まで無線回路を休ませます。

#### 31バイトに発見の手がかりを入れる

Legacy Advertisingの`ADV_IND`パケットは、次の要素で構成されます。

| 要素 | サイズ | 役割 |
|---|---:|---|
| Preamble | 1バイト | 受信の準備とビット同期 |
| Access Address | 4バイト | アドバタイジング用の固定値 |
| Link Layer Header | 2バイト | PDUの種類や長さ |
| AdvA | 6バイト | アドバタイザーのデバイスアドレス |
| Advertising Data | 0〜31バイト | デバイスを見つけ、選ぶための情報 |
| CRC | 3バイト | 受信したビット列の誤り検出 |

: Legacy AdvertisingのADV_INDパケット {#tbl:legacy-adv-ind-packet}

<!--
図版プレースホルダー:
- 目的: Legacy Advertisingパケット全体と、ファームウェアが構成するAdvertising Dataの範囲を一目で区別する。
- 要素: Preamble 1 B、Access Address 4 B、Header 2 B、AdvA 6 B、Advertising Data 0〜31 B、CRC 3 B。Header以下のPDU payloadは最大37 Bであり、その内訳がAdvA 6 BとAdvertising Data最大31 Bであることを括弧で示す。
- 強調: Advertising Dataだけを色以外に太枠でも示し、「ファームウェアが内容を選ぶ最大31 B」と注記する。
- 制作: 外部作図ツールで編集可能な原本を保存し、sRGB、白背景、横1800 px以上のPNGへ書き出す。
- 予定画像: images/diagrams/ch02-legacy-advertising-packet.png
- ラベル: fig:legacy-advertising-packet
- キャプション: Legacy AdvertisingのADV_INDパケットとAdvertising Dataの範囲
- alt: ADV_INDパケットをPreamble、Access Address、Header、AdvA、最大31バイトのAdvertising Data、CRCに分けた構造図。
- 有効化時の本文: Legacy Advertisingでファームウェアが内容を選ぶ範囲を、[@fig:legacy-advertising-packet]に示します。
-->

Link LayerのPDU payloadとしては、AdvAとAdvertising Dataを合わせて最大37バイトです。このうちAdvAの6バイトを除いた最大31バイトが、一般に「Legacy Advertisingのアドバタイジングデータ」と呼ばれる範囲です。ファームウェアは、製品を見つけてもらうための情報を、この31バイトへ取捨選択して入れます。

Advertising Dataの中には、AD Structure（Advertising Data Structure）を必要な数だけ並べます。一つのAD Structureは、長さを示す`Length`、情報の種類を示す`AD Type`、実際の値である`AD Data`から構成されます。

```text
Length | AD Type | AD Data
Length | AD Type | AD Data
...
```

代表的なAD Typeには、次の情報があります。

- Flags：利用するLEの機能や発見モード
- Service UUID：接続後に利用できるサービスの手がかり
- Local Name：画面へ表示する名前
- Manufacturer Specific Data：企業ごとに定義するデータ
- Service Data：特定のサービスUUIDに対応するデータ
- Tx Power Level：パケット送信時の送信電力
- Appearance：機器の種類を表す値

`Length`と`AD Type`にもバイトを使うため、31バイトすべてを値へ使えるわけではありません。長いLocal Nameを入れると、それだけでほかの情報を入れる余地が減ります。接続先を選ぶために必須のService UUIDをAdvertising Dataへ入れ、表示名などを後述するScan Responseへ移す、といった割り振りが必要になります。

#### ビーコンも同じ範囲を使う

ビーコンは、Advertising Dataへ識別情報などを入れ、接続せずに繰り返し送る利用方法です。ビーコン専用の電波や別のパケットがあるわけではありません。

Appleが公開している入門資料では、iBeaconのアドバタイジングにUUID、Major、Minorを入れます。UUIDで一群のビーコンを識別し、MajorとMinorで場所や個体を段階的に分けられます。受信側のiOSデバイスは、Core Locationを使ってビーコン領域への出入りや大まかな近接状態を扱います。Manufacturer Specific Data内の正確なバイト配置など、iBeacon対応製品を製造するための詳細は、Appleからライセンス提供されるProximity Beacon Specificationの範囲です。本節では、公開資料で確認できる識別情報と利用方法だけを扱います。

#### アドバタイジング間隔をファームウェアで決める

アドバタイジングは一度だけではなく、Advertising Interval（アドバタイジング間隔）ごとに繰り返します。この間隔は、周辺機器のファームウェアからBLEスタックへ設定する製品パラメーターです。Hostは、希望する間隔の最小値と最大値をControllerへ設定します。Legacy Advertisingでは20 msから10.24 sまで指定できるため、実際の製品でも数十 msから数 sまで幅広い値が使われます。

間隔を短くするとスキャナーが受信する機会は増えますが、周辺機器が無線回路を動かす回数も増えます。間隔を長くすると待機中の消費電力を抑えられる一方、アプリケーションが機器を見つけるまでの時間が長くなります。ここで決めているのは通信速度ではなく、発見される機会と待機中の消費電力です。接続後のコネクションインターバルとは別の値です。

Bluetooth Core SpecificationのGAPには、この二つを切り替える推奨例があります。LE 1M PHYで接続可能なAdvertisingを行う場合は、ユーザー操作によってAdvertisingを開始した少なくとも最初の30 sを30〜60 ms、その後のバックグラウンドAdvertisingを1〜1.2 sとしています。

```text
ユーザーが電源を入れる、または接続操作をする
    ↓
最初の30 s
    Advertising Interval: 30〜60 ms
    発見の速さを優先する
    ↓
30 sを過ぎても接続されない
    Advertising Interval: 1〜1.2 s
    待機中の消費電力を優先する
```

これはすべての製品にそのまま適用する必須値ではなく、GAPが示す推奨テンプレートです。たとえば、ボタンを押した直後だけ短い間隔に戻す、一定時間が経過したらAdvertising自体を停止する、といった製品の振る舞いを設計できます。

実際のアドバタイジングイベント間隔には、設定したAdvertising Intervalに0〜10 msのランダムな遅延が加わります。複数のアドバタイザーが同じ周期で動いても、送信時刻が重なり続けることを避けるためです。

<!--
技術確認:
- Bluetooth Core Specification v6.3, Vol 1, Part A, Section 2
- Bluetooth Core Specification v6.3, Vol 3, Part C, Section 11
- Bluetooth Core Specification v6.3, Vol 3, Part C, Section 9.3.11 and Appendix A
- Bluetooth Core Specification v6.3, Vol 4, Part E, Section 7.8.5
- Bluetooth Core Specification v6.3, Vol 6, Part B, Section 1.4, Section 2.1, Section 2.3.1.1, and Section 4.4
- 旧稿 hoge/04_LinkLayer.re のアドバタイジング説明を構成素材として参照
- Apple, Getting Started with iBeacon, Version 1.0（参照日: 2026-07-29）
- Apple, iBeacon（参照日: 2026-07-29）
-->
