## コントローラとホスト——2つのブロック {#sec:stack-overview}

<!-- topic: コントローラとホストの分離 -->

<!--
統合予定: 第2章2.1から移した「実装軸」の説明。

第3章をoutline.mdの方針に沿って全面改稿するとき、現在の「1チップ構成／2チップ構成」と重複を整理し、HCI境界を説明する本文へ統合する。

伝えたいこと:
- レイヤー構造は、複雑な通信規格を責務ごとに定義する構造でもある。
- ControllerはPHYとLink Layerを担当し、無線回路の近くでマイクロ秒単位の精度が必要な処理を実行する。
- HostはL2CAP、ATT、GATT、GAP、SMPなどを担当し、無線の一回一回を直接制御せず、主にミリ秒から秒単位で進む通信手順を管理する。
- HCIを規格として定めることで、Bluetooth半導体側のControllerと、OSやファームウェア側のHostを独立して実装できる。
- HostとControllerは別のICにも、同じBluetooth SoC内にも配置できる。物理構成にかかわらず、仕様上の責務とデータの流れはHCIを境に分かれる。

本文候補:

BLEの機能をレイヤーに分ける理由は、データの経路を説明するためだけではありません。無線通信に必要な機能を責務ごとに分け、それぞれのパケット形式、手順、状態を規格として定義するための構造でもあります。

一つの機器のプロトコルスタックは、大きくHostとControllerに分かれます。ControllerはPHYとLink Layerを担当し、決められた時刻とチャンネルでパケットを送受信します。無線を開始する時刻、相手からのパケットに応答する間隔、受信確認と再送など、マイクロ秒単位の精度が必要になる処理を無線回路の近くで実行します。この部分は、Bluetooth対応の無線半導体が提供する機能に対応します。

Hostは、L2CAP、ATT、GATT、GAP、SMPなどを担当します。サービスを探索する、キャラクタリスティックを読み書きする、ペアリングを進めるといった通信手順を管理します。Controllerのように無線の一回一回をマイクロ秒単位で直接制御するのではなく、主にミリ秒から秒単位で進む手順を扱い、OSやファームウェアのBLEスタックとして実装されます。

HostとControllerの境界が、HCI（Host Controller Interface）です。Hostは、送信するデータや無線の設定をHCIのコマンドやデータとしてControllerへ渡します。Controllerは、Link Layerの時刻に合わせてパケットを交換し、結果をHCIのイベントやデータとしてHostへ返します。Hostが無線の一回一回をマイクロ秒単位で指示する必要はありません。

HCIを規格として定めることで、Hostを実装するOSと、Controllerを実装するBluetooth半導体を独立して開発できます。異なるメーカーが作ったHostとControllerでも、同じHCIの契約に従えば組み合わせられます。パソコンとUSB Bluetoothアダプターのように物理的に分離することも、Bluetooth SoCの内部へHostとControllerを収めることもできます。1チップに収まっている場合でも、仕様上の責務とデータの流れはHCIを境に分かれています。
-->

BLEプロトコルスタックは、大きく**コントローラ（Controller）**と**ホスト（Host）**に分かれています。

```
┌──────────────────────────────────────────────────┐
│  アプリケーション                                 │
├──────────────────────────────────────────────────┤
│  ホスト（Host）                                   │
│  ┌─────────────────────────────────────────────┐ │
│  │ GATT（Generic Attribute Profile）           │ │
│  │ ATT （Attribute Protocol）                  │ │
│  │ SMP （Security Manager Protocol）           │ │
│  │ GAP （Generic Access Profile）              │ │
│  │ L2CAP（Logical Link Control and Adaptation）│ │
│  └─────────────────────────────────────────────┘ │
├──────────────── HCI（Host Controller Interface）──┤
│  コントローラ（Controller）                       │
│  ┌─────────────────────────────────────────────┐ │
│  │ リンク層（Link Layer）                       │ │
│  │ 物理層（Physical Layer）                     │ │
│  └─────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
```

コントローラはアンテナから電波を送受信し、パケットの送受信タイミングをマイクロ秒単位で管理します。物理層とリンク層で構成され、BLEチップ内部（ハードウェアとファームウェア）で実装されます。リアルタイム性が要求されるため、処理がソフトウェアで上position できないよう、専用のICに閉じて実装されているのが一般的です。

ホストは、コントローラが確立した通信路の上でデータをどう表現し、どう運ぶかを担います。セキュリティ管理やGATTのデータベース構造も含め、スマートフォンのOSや組み込みRTOS上で動作するソフトウェアとして実装されます。

コントローラとホストの境界には**HCI（Host Controller Interface）**があります。この境界を明示しているのは、2つを異なるチップに分けた構成が現実にあるからです。

**1チップ構成**では、コントローラとホストが同じ半導体チップ内で動作します。HCIはソフトウェア間の論理的な接続として機能します。nRF52840のようなマイコンでは、チップ内部のマイクロコントローラがコントローラとホストの両方を実行し、ユーザのアプリケーションも同じチップで動きます。機能が単純な周辺機器では、製造コストを最小にするためにこの構成が取られます。

**2チップ構成**では、コントローラが無線モジュールとして部品化され、ホストは別のプロセッサで実装されます。両者はUARTやSPI、USBなどの物理インタフェースを通じてHCIで通信します。電波を出力するモジュールが各国の電波法の承認を取得していれば、機器本体での承認取得が不要になる利点があります。モバイル機器やパソコンでBluetooth USBドングルを使う場合も、おのずとこの構成になります。

規格にHCIがあるので、2チップ構成でも、どのメーカのコントローラでも共通に使えるようになります。ただし、ホストとユーザアプリケーションの間のインタフェースは規格に定義がありません。iOSのCore Bluetooth、AndroidのBluetooth API、ZephyrのBluetooth APIなど、プラットフォームごとに異なります。


## 各層の役割——一言で整理する

<!-- topic: 各層の責務の一言説明 -->

| 層 | 略称 | 一言の役割 |
|---|---|---|
| 物理層 | PHY | 電波のビット送受信（変調・復調・チャンネル管理） |
| リンク層 | LL | パケット管理・接続確立・状態管理・周波数ホッピング |
| ホスト側インタフェース | HCI | コントローラとホストの境界（コマンド/イベント/データ） |
| 論理リンク制御 | L2CAP | パケットの多重化・細分化（上位プロトコルを仮想チャンネルで分離） |
| セキュリティマネージャ | SMP | ペアリング・鍵交換・暗号化のプロシージャ |
| 汎用アクセスプロファイル | GAP | デバイスの発見・接続・振る舞いのシナリオ定義（層横断） |
| 属性プロトコル | ATT | ハンドル番号でアクセスする読み書きコマンド集 |
| 汎用属性プロファイル | GATT | ATTの上に階層、サービスとキャラクタリスティックのデータ構造定義 |

GAPは少し特殊な存在です。通信プロトコルの「一層」ではなく、リンク層からGATTまでをまたいで「どのようにデバイスを発見・接続するか」という振る舞いのシナリオを定義するプロファイルです。[@sec:ch2]で使った「セントラル」「ペリフェラル」という用語は、このGAPが定義したロールです。GAPの詳細は[@sec:gap]で扱います。


## この章と第4章でカバーする範囲

<!-- topic: 章とスタックの対応を明示する -->

この章では**物理層・リンク層・GAP**を扱います。電波のレベルから接続確立まで、コントローラ側の仕組みを中心に読み解きます。

[@sec:ch4]では**ATT・GATT**を扱います。確立した接続の上で、データをどのように表現・操作するかを解説します。

L2CAPとSMPについては、この章と[@sec:ch4]でそれぞれ役割に応じて触れますが、独立した節としては扱いません。


## 第2章の通信フローをスタックに対応させる

<!-- topic: 第2章フローとスタックの対応 -->

[@sec:ch2]で学んだ通信フローは、スタックの具体的な層に対応しています。

| 第2章のフェーズ | 担う主な層 | この章での対応節 |
|---|---|---|
| 通信路を構成する用語とデータ経路 | スタック全体 | 3.2 |
| アドバタイジング・スキャン | 物理層 / リンク層 / GAP | 3.3 / 3.4 / 3.5 / 3.7 |
| 接続確立（CONNECT\_IND） | リンク層 | 3.6 |
| サービス探索 / データ交換 | ATT / GATT | 第4章 |
| ペアリング / ボンディング | SMP / GAP | 第4章（SMP）/ 3.7（GAPシナリオ） |

次節では、物理層へ進む前に、チャネル、リンク、トランスポートという通信路の名前を整理します。
